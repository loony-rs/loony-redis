[package]
name = "metrics"
version = "0.1.0"
edition = "2021"

[dependencies]
prometheus = "0.13"
tokio = { version = "1", features = ["net", "io-util", "rt", "macros"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
